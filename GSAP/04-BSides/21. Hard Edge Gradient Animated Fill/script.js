/* Start lesson at 6:30 */

const tween = gsap.to("li", {
  
});

GSDevTools.create({ animation: tween });



/* COMPLETE

const tween = gsap.to("li", {
  backgroundImage:
    "linear-gradient(90deg, var(--left) 0%, var(--left) 100%, var(--right) 100%)",
  duration: 0.3,
  ease: "power1",
  stagger: {
    each: 0.1,
    ease: "power2.out"
  }
});

GSDevTools.create({ animation: tween });

*/