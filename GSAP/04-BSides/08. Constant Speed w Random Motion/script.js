/* just watch the 1st short demo with blue and pink bug  */

gsap.defaults({ease:"none"});

let pxPerSecond = 100

function moveMe(target) {

	let curPos = {
		
	}

	let newPos = {
		
	}

	let deltaX = null;
	let deltaY = null;
	let distance = null;

	let duration = null;
	
	 
	
}

 moveMe(".bug")

document.addEventListener("click", () => gsap.globalTimeline.paused(!gsap.globalTimeline.paused()))

// GSDevToos.create();