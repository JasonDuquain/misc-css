A Pen created at CodePen.io. You can find this one at http://codepen.io/tystrong/pen/azdoWE.

 The clip-path property prevents a portion of an element from drawing by defining a clipping region. This way we can make different shapes out of HTML elements. The clip-path property can be applied to all HTML elements, SVG graphic elements and SVG container elements.