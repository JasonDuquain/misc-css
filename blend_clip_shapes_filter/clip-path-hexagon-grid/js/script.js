/*

const gridItems = document.querySelectorAll('.grid--item:not(.no)');


window.addEventListener('resize', function(e) {
  gridItems.forEach((el, idx) => {
    const width = el.getBoundingClientRect().width;
    
    console.log(typeof width)
    
    el.style.marginTop = `-${width * .257}px`;
    el.style.marginBottom = `${width * .257}px`;
    
    if (el.classList.contains('double')) {
      el.style.marginTop = `-${(width * 2) * .257}px`;
      el.style.marginBottom = `${(width * 2) * .257}px`;
    }
    
  })
});

*/





