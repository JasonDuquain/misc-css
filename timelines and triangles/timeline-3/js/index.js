(function() {
    "use strict";
    
    
    
    
    
    
    
})();







































/*

(function() {
    "use strict";
    
    let content = document.querySelectorAll('[class$="-container"]');

    function callbackFunction() {
        content.forEach((el) => {
            if (isElementInViewport(el)) {
                el.classList.add('slidein');
            }
        });    
    }


    function isElementInViewport(el) {
        let elem = el.getBoundingClientRect();

        return (
            elem.top >= 0 && elem.bottom <= (window.innerHeight || document.documentElement.clientHeight)
        )
    }

    window.addEventListener('scroll', callbackFunction);
    window.addEventListener('load', callbackFunction);
    window.addEventListener('resize', callbackFunction);

    
    
})();




*/